# Public @model-match packages publish to npmjs.org (same as @model-match/api).
# NPM_PUBLISH_TOKEN is injected by the Publish workflow; empty locally, which is
# fine for installing public packages.
//registry.npmjs.org/:_authToken=${NPM_PUBLISH_TOKEN}
