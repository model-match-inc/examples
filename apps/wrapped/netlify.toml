# Generated by @model-match/create — edit template.json then `npm run gen`.

[build]
  command = "npm run build"
  publish = "dist"

[template.environment]
  MODEL_MATCH_API_KEY = "Create one in the Model Match dashboard. Sent as the x-api-key header, server-side only — it is never exposed to the browser."
  MODEL_MATCH_NMLS_ID = "The loan originator whose year gets wrapped (e.g. 1036297)."
  MMR_API_URL = "Model Match REST API base URL"
  REPORT_YEAR = "Year to review"
