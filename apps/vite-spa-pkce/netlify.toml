# Generated by @model-match/create — edit template.json then `npm run gen`.

[build]
  command = "npm run build"
  publish = "dist"

[template.environment]
  VITE_MODEL_MATCH_CLIENT_ID = "A public OAuth client — there is NO secret. This value is inlined into the browser bundle, which is fine for a PKCE client."
  VITE_MODEL_MATCH_ISSUER = "The SDK appends /oauth2/authorize and /oauth2/token to this value. Point it at the auth base, NOT the bare host (the bare host 404s — endpoints live under /api/auth)."
  VITE_MMR_API_URL = "Model Match REST API base URL"
  VITE_MODEL_MATCH_REDIRECT_URI = "Must exactly match the redirect URI registered with your OAuth app."
