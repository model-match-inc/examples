# Generated by @model-match/create — edit template.json then `npm run gen`.

[build]
  command = "npm run build"
  publish = "dist"

[template.environment]
  MODEL_MATCH_ISSUER = "The SDK appends /oauth2/authorize and /oauth2/token to this value. Point it at the auth base, NOT the bare host (the bare host 404s — endpoints live under /api/auth)."
  MMR_API_URL = "Model Match REST API base URL"
  MODEL_MATCH_CLIENT_ID = "OAuth client ID"
  MODEL_MATCH_CLIENT_SECRET = "NEVER prefix this with VITE_ — it must never reach the browser bundle."
  MODEL_MATCH_REDIRECT_URI = "Must exactly match the redirect URI registered with your OAuth app."
